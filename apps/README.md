# Applications

This directory owns deployable application boundaries. The current API and worker implementation lives inside `packages/platform-core` until the next extraction can preserve packaging, imports, deployment and CI invariants.
