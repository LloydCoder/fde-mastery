# Domains

Production domain adapters remain governed by the platform kernel and are currently contained within `packages/platform-core/domains` to preserve package and runtime invariants.

Historical learning curriculum is separated under `legacy/curriculum` and is not part of the production dependency graph.
