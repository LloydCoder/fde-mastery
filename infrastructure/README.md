# Infrastructure

Repository-level infrastructure ownership belongs here. The current Terraform, deployment manifests and runtime scripts remain co-located with `packages/platform-core` so the production build context stays self-contained. They are governed by the same CI, SBOM and deployment gates.
